 # Js and classes

## OOP
-Programming paradigm => style of writing code == 'structure'

## Object
- collection of prpts (variables) & methods (func)
-toLowerCase, toLowerCase, even Promises ('new' keyword creates new instance)=> also are basically objects doing our work

## Why use OOP?

## Parts of OOP
1) object literal- means: object only ie., literally 1 object banaana h=>  {}

2) Constructor func
3) Prototypes
4) Classes
5) Instances (new, this keyword)

## 4 pillars
-Abstraction- (hiding details)
-Encapsulation- (wrapping up of data into a single unit)
-Inheritance
-Polymorphism- (poly: 'many' & morph:'forms')